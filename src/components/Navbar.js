// src/components/Navbar.js
import React from 'react';

const Navbar = () => {
    return (
        <nav>
            <h2>Brand Name</h2>
            {/* Add links if needed */}
        </nav>
    );
};

export default Navbar;
